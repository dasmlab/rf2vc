# Minimal gateway config. vCenters + UUID map live on PVC at dataDir/state.json
# (managed via the admin UI). Optional GOVC_* env seeds the first vCenter.

listen: ":8080"
tlsCertFile: ""
tlsKeyFile: ""

auth:
  username: "redfish"
  password: "CHANGE_ME"

dataDir: "./data"
isoCacheDir: "./data/iso-cache"
